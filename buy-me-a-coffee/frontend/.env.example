# Frontend environment variables for the Buy Me a Coffee dApp
#
# Copy this file to `.env.local` for local development, or
# add the same keys in Vercel under:
#   Project Settings → Environment Variables
#
# Base URL of the backend API. In production this will usually be your
# backend Vercel project (or the same domain if you route /api/* there).
# If this is not set, the app falls back to window.location.origin.
VITE_API_BASE_URL=http://localhost:4000